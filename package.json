{
  "name": "talentia-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rmdir /s /q dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.276",
    "cpx": "^1.5.0",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5"
  }
}
